interface Note{
    tags: Tag[]
}
interface Tag{
    id?: number;
    name: string;
}
const a: Note ={
    tags: [{name: 'wsei', id: 12}, {name: 'poniedzialek'}]
}
//a.tags[0].id = 12

cnst tags = req.body.tags
tags.gorEach(element => {


});
const str = 'some strin'
const strToLower = str.toLowerCase();
